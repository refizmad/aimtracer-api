# On the Windows render PC only (NOT Coolify).
# cmd / PowerShell / Task Scheduler / M7 service wrapper.

AIMTRACE_API=https://api.aimtracer.com
MACHINE_TOKEN=

# S3 upload stays local to the worker (write keys) — s3_config.json or:
# S3_ENDPOINT_URL=
# S3_BUCKET=aimtracer-clips
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_PRESIGN=1
